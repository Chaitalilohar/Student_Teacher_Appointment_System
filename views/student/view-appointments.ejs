<!-- views/student/view-appointments.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Appointments</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head><style>
    .back-button {
        display: inline-block;
        padding: 0.5rem 1rem;
        background-color: #00450e; /* Tailwind's blue-500 */
        color: white;
        border-radius: 0.375rem; /* Tailwind's rounded-md */
        text-align: center;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }
    
    .back-button:hover {
        background-color: #034b14; /* Tailwind's blue-600 */
    }
</style>
<script>
    function goBack() {
        window.history.back();
    }
</script>
<body class="bg-gray-100">
    <div class="container mx-auto p-4  items-center">
        <!-- Your content here -->
        <button class="back-button" onclick="goBack()">Go Back</button>
    </div>

    <div class="max-w-7xl mx-auto py-12 px-4 sm:px-6 lg:px-8">
        <h1 class="text-3xl font-bold mb-6">Appointments</h1>

        <div class="overflow-x-auto bg-white shadow-md rounded-lg">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-green-900 text-white">
                    <tr>
                        <th class="px-6 py-3 text-left text-sm font-medium uppercase tracking-wider">Teacher Name</th>
                        <th class="px-6 py-3 text-left text-sm font-medium uppercase tracking-wider">Time</th>
                        <th class="px-6 py-3 text-left text-sm font-medium uppercase tracking-wider">Message</th>
                        <th class="px-6 py-3 text-left text-sm font-medium uppercase tracking-wider">Status</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-200">
                    <% appointments.forEach(appointment => { %>
                        <tr class="bg-white">
                            <td class="px-6 py-4 whitespace-nowrap"><%= appointment.teacherId ? appointment.teacherId.name : 'Unknown' %></td>
                            <td class="px-6 py-4 whitespace-nowrap"><%= appointment.time ? new Date(appointment.time).toDateString() : 'Unknown' %></td>
                            <td class="px-6 py-4 whitespace-nowrap"><%= appointment.message %></td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full 
                                    <% if (appointment.status === 'pending') { %>
                                        bg-yellow-500 text-white
                                    <% } else if (appointment.status === 'approved') { %>
                                        bg-green-500 text-white
                                    <% } else if (appointment.status === 'cancelled') { %>
                                        bg-red-500 text-white
                                    <% } %>">
                                    <%= appointment.status %>
                                </span>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
